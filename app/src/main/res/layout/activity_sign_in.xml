<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_sign_in"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="alexparunov.lookaround.accounts.SignIn">

    <Button
        android:id="@+id/activity_sign_in_B"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/sign_in"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="191dp" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textEmailAddress"
        android:ems="10"
        android:layout_above="@+id/activity_sign_in_B"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="62dp"
        android:id="@+id/activity_sign_in_usernameEmailET"
        android:hint="Email" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:ems="10"
        android:layout_above="@+id/activity_sign_in_B"
        android:layout_alignEnd="@+id/activity_sign_in_usernameEmailET"
        android:layout_marginBottom="19dp"
        android:id="@+id/activity_sign_in_passwordET"
        android:hint="Password" />

    <TextView
        android:text="Not a member ? Sign Up"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/activity_sign_in_usernameEmailET"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="56dp"
        android:textSize="20dp"
        android:id="@+id/activity_sign_in_signUpTV" />

</RelativeLayout>
